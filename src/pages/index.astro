---
import Layout from '../layouts/Layout.astro';
import AsciiPortrait from '../components/AsciiPortrait';
import ThemeToggle from '../components/ThemeToggle.astro';

// Il ritaglio è già esportato alla misura esatta che serve (2x di 420px).
// Passando per `import` di src/assets, Astro emetteva anche il master PNG da
// 1,1 MB pur non servendolo a nessuno.
const PORTRAIT = { src: '/simone-hero.webp', width: 840, height: 941 };

// TODO: contenuti da sostituire con i tuoi dati reali.
// Un accento per strato: i tre neon non sono decorazione, distinguono i piani.
const stack = [
  {
    layer: 'Interfaccia',
    role: 'quello che si vede',
    accent: 'text-fuchsia',
    rule: 'bg-fuchsia',
    tools: ['TypeScript', 'React', 'Astro', 'Tailwind', 'Flutter'],
  },
  {
    layer: 'Logica e dati',
    role: 'quello che decide',
    accent: 'text-cyan',
    rule: 'bg-cyan',
    tools: ['Node.js', 'API REST', 'PostgreSQL', 'Autenticazione'],
  },
  {
    layer: 'Infrastruttura',
    role: 'quello che regge',
    accent: 'text-lime',
    rule: 'bg-lime',
    tools: ['Git', 'Docker', 'CI/CD', 'Deploy continuo'],
  },
];

const projects = [
  {
    name: 'Critical Table',
    year: '2024',
    kind: 'Back end · Web · Mobile',
    description:
      'Strumento per sessioni di gioco di ruolo da tavolo, portato avanti su tre codebase che parlano la stessa API.',
  },
  {
    name: 'Linda',
    year: '2025',
    kind: 'App mobile',
    description:
      'Pulizia della galleria fotografica. Nata da un problema mio: dodicimila foto e nessuna voglia di scorrerle.',
  },
  {
    name: 'Listino Prezzi',
    year: '2025',
    kind: 'Gestionale su misura',
    description:
      'Un gestionale piccolo ma reale, costruito perché un’attività smettesse di lavorare a fogli di calcolo.',
  },
];
---

<Layout
  title="Simone Potenza — Full stack developer"
  description="Front end, back end e tutto quello che sta in mezzo. Portfolio di Simone Potenza, full stack developer."
>
  <header
    class="reveal sticky top-0 z-20 flex items-center justify-between border-b border-line bg-canvas/85 px-6 py-4 backdrop-blur md:px-12"
  >
    <span class="font-mono text-xs tracking-[0.2em] text-dim uppercase">Simone Potenza</span>
    <div class="flex items-center gap-4">
      <a
        href="#contatti"
        class="font-mono text-xs tracking-[0.2em] text-fuchsia uppercase underline-offset-4 transition hover:underline"
      >
        Scrivimi
      </a>
      <ThemeToggle />
    </div>
  </header>

  <main class="px-6 md:px-12">
    <!-- Il sito riprende esattamente la frase con cui è stato consigliato. -->
    <section
      class="mx-auto grid min-h-[80vh] max-w-6xl items-center gap-14 py-20 md:grid-cols-[1.1fr_0.9fr] md:gap-16"
    >
      <div>
        <p
          class="reveal font-mono text-xs tracking-[0.24em] text-lime uppercase"
          style="--reveal-delay: 80ms"
        >
          Full stack developer
        </p>

        <h1
          class="reveal mt-6 font-display text-[clamp(2.4rem,5.6vw,4.6rem)] leading-[0.98] font-extrabold tracking-tight text-balance"
          style="--reveal-delay: 160ms"
        >
          Front end, back end, e <span class="glow text-fuchsia">tutto quello che sta in mezzo</span
          >.
        </h1>

        <p
          class="reveal mt-8 max-w-xl text-lg leading-relaxed text-dim"
          style="--reveal-delay: 260ms"
        >
          Costruisco prodotti interi: l’interfaccia con cui la gente ci parla e il sistema che la
          tiene in piedi. Di solito la parte difficile è farli combaciare.
        </p>

        <div class="reveal mt-10 flex flex-wrap items-center gap-6" style="--reveal-delay: 340ms">
          <a
            href="#progetti"
            class="rounded-full border border-line px-6 py-3 font-medium text-fg transition hover:border-cyan hover:text-cyan"
          >
            Guarda i progetti
          </a>
          <a
            href="#contatti"
            class="text-dim underline underline-offset-4 transition hover:text-fg"
          >
            Parliamone
          </a>
        </div>
      </div>

      <!--
        Il pulsante sta sotto la foto, non sotto il testo: comanda l'immagine,
        e tenerlo accanto a ciò che governa evita di doverlo spiegare.
      -->
      <div class="reveal justify-self-center md:justify-self-end" style="--reveal-delay: 420ms">
        <AsciiPortrait
          client:load
          src={PORTRAIT.src}
          width={PORTRAIT.width}
          height={PORTRAIT.height}
          alt="Simone Potenza"
        />
      </div>
    </section>

    <!--
      Le competenze sono impilate come lo stack vero: dall'alto ciò che si
      vede, in fondo ciò che regge il peso. L'ordine è informazione, e il
      colore distingue il piano.
    -->
    <section class="mx-auto max-w-6xl border-t border-line py-20">
      <h2 class="font-mono text-xs tracking-[0.24em] text-dim uppercase">Lo stack</h2>

      <div class="mt-10 flex flex-col">
        {
          stack.map((level) => (
            <div class="grid gap-4 border-b border-line py-8 last:border-b-0 md:grid-cols-[1fr_2fr] md:gap-10">
              <div>
                <span class:list={['mb-4 block h-px w-16', level.rule]} />
                <h3 class:list={['font-display text-3xl font-bold tracking-tight md:text-4xl', level.accent]}>
                  {level.layer}
                </h3>
                <p class="mt-1 text-sm text-dim">{level.role}</p>
              </div>
              <ul class="flex flex-wrap items-start gap-2 self-center">
                {level.tools.map((tool) => (
                  <li class="rounded-full border border-line px-4 py-1.5 font-mono text-sm text-dim">
                    {tool}
                  </li>
                ))}
              </ul>
            </div>
          ))
        }
      </div>
    </section>

    <section id="progetti" class="mx-auto max-w-6xl border-t border-line py-20">
      <h2 class="font-mono text-xs tracking-[0.24em] text-dim uppercase">Progetti</h2>

      <div class="mt-10 flex flex-col">
        {
          projects.map((project) => (
            <article class="group grid gap-3 border-b border-line py-8 last:border-b-0 md:grid-cols-[1fr_2fr] md:gap-10">
              <div>
                <h3 class="font-display text-3xl font-bold tracking-tight transition group-hover:text-cyan md:text-4xl">
                  {project.name}
                </h3>
                <p class="mt-1 font-mono text-xs tracking-wider text-dim uppercase">
                  {project.year} · {project.kind}
                </p>
              </div>
              <p class="max-w-2xl self-center text-lg leading-relaxed text-dim">
                {project.description}
              </p>
            </article>
          ))
        }
      </div>
    </section>

    <section id="contatti" class="mx-auto max-w-6xl border-t border-line py-20">
      <h2 class="font-mono text-xs tracking-[0.24em] text-dim uppercase">Contatti</h2>

      <p
        class="mt-8 max-w-2xl font-display text-3xl leading-tight font-bold text-balance md:text-5xl"
      >
        Se stai cercando qualcuno che ti costruisca la cosa intera, <span
          class="glow text-cyan">scrivimi</span
        >.
      </p>

      <div class="mt-10 flex flex-wrap gap-x-10 gap-y-4">
        <a
          href="mailto:simonepotenzatr@gmail.com"
          class="text-lg text-fuchsia underline underline-offset-4 transition hover:text-fg"
        >
          simonepotenzatr@gmail.com
        </a>
        <a
          href="https://github.com/SimonePotenza1006"
          class="text-lg text-fuchsia underline underline-offset-4 transition hover:text-fg"
        >
          GitHub
        </a>
      </div>
    </section>
  </main>

  <footer class="border-t border-line px-6 py-10 md:px-12">
    <p class="mx-auto max-w-6xl font-mono text-xs tracking-wider text-dim">
      Costruito con Astro. Statistiche di visita anonime, nessun cookie.
    </p>
  </footer>
</Layout>
